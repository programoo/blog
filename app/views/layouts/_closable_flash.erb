<% flash.each do |key, value| %>
  <div class="alert alert-<%= key == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show m-0" role="alert">
    <%= value %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>