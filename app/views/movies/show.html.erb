<div class="container my-5">
  <div class="row">
    <!-- Left Section: Image + Details -->
    <div class="col-md-6 mb-4">
      <div class="card shadow-sm">
        <div class="row g-0 h-100">
          <div class="col-6 d-flex align-items-stretch">
            <% if @movie.images.attached? %>
              <%= image_tag @movie.images.first.variant(resize_to_fill: [900, 500]),
                      class: "img-fluid rounded-0", alt: @movie.title %>
            <% elsif @movie.source_image.present? %>
              <%= image_tag @movie.source_image,
                      class: "img-fluid rounded-0", alt: "Source Image", size: "700x500" %>
            <% else %>
              <%= image_tag "placeholder.png",
                      class: "img-fluid rounded-0", alt: "No image" %>
            <% end %>
          </div>
          <div class="col-6">
            <div class="card-body">
              <h2 class="card-title mb-3 fw-bold text-primary"><%= @movie.title %></h2>
              <div class="mb-4">
                <p class="card-text text-muted fst-italic"><%= @movie.description %></p>
              </div>
              <h5 class="mb-3 text-secondary">🎬 รายละเอียด</h5>
              <ul class="list-group list-group-flush border rounded-3">
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <span class="fw-semibold">เปิดตัววันแรก:</span>
                  <span><%= @movie.release_date %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <span class="fw-semibold">Category:</span>
                  <span class="badge bg-info text-dark"><%= @movie.category %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <span class="fw-semibold">ความยาว:</span>
                  <span><%= @movie.duration %></span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- Right Section: YouTube Video -->
    <div class="col-md-6 mb-4">
      <div class="ratio ratio-16x9">
        <iframe src="https://www.youtube.com/embed/43R9l7EkJwE" 
            title="YouTube video" 
            allowfullscreen>
        </iframe>
      </div>
    </div>
    <div class="card-footer bg-white">
      <%= render "shared/social_panel", movie: @movie %>
    </div>
  </div>
</div>