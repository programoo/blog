<div class="container d-flex justify-content-center align-items-center vh-100">
  <div class="card shadow-sm p-4" style="max-width: 400px; width: 100%;">
    <h2 class="text-center mb-4">ลืมรหัสผ่าน?</h2>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, data: { turbo: false }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="mb-3">
        <%= f.label :email, "อีเมล", class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "กรอกอีเมลของคุณ" %>
      </div>
      <div class="d-grid">
        <%= f.submit "ส่งคำแนะนำการรีเซ็ตรหัสผ่าน", class: "btn btn-primary" %>
      </div>
      <div class="mt-3 text-center">
        <%= link_to "กลับไปหน้าเข้าสู่ระบบ", new_session_path(resource_name), class: "text-decoration-none" %>
      </div>
    <% end %>
  </div>
</div>
